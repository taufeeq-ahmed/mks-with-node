describe("catalog.service.tests", () => {
  beforeEach(() => {});

  afterEach(() => {});

  test("create-product", () => {
    const a = 10;
    expect(a).toEqual(10);
  });

  test("create-product:throws error if it already exists", () => {
    const a = 10;
    expect(a).toEqual(10);
  });
});
